[project]
name = "ti-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyodbc>=5.3.0",
    "ta-lib>=0.6.8",
    "yfinance>=0.2.66",
]

[project.scripts]
ti = "ti.ti:main"